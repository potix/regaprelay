package gamepad

/*

 gamepad frontend 

        // setup gamepad
        gVerboseOpt := gamepad.GamepadVerbose(conf.Verbose)
        newGamepad, err := gamepad.NewGamepad(conf.Gamepad.Model, gVerboseOpt)
        if err != nil {
                log.Fatalf("can not create gamepad: %v", err)
        }

func (t *TcpHandler) onVibration(handler.GamepadVibration) {
        // XXX
        // vibration information
        // 1. make vibration request data
        // 2. encode to json
        // 3. send json by safeConnWrite
        // 4. loggin when encount error
}

func (t *TcpClient) Start() error {
        t.gamepad.StartVibrationListener(t.onVibration)
        go t.reconnectLoop()
}

t.gamepad.UpdateState(msg.GamepadState)


*/
